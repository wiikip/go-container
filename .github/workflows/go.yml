name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up buildx
        uses: docker/setup-buildx-action@v1

      - name: Build and push docker
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: wiikip/go-container:$GITHUB_SHA
